// Inicio

// Declaración de variables
const nota1 = 4.5;
const nota2 = 3.8;
const nota3 = 2.9;
const nota4 = 4.0;
console.log("Las notas son:", nota1, nota2, nota3, nota4);

let suma, promedio, expresion;

// Cálculo de la suma de las notas
suma = nota1 + nota2 + nota3 + nota4;
console.log("La suma de las notas es:", suma);

// Cálculo del promedio de las notas
promedio = suma / 4;
console.log("El promedio de las notas es:", promedio);

// Evaluación de la expresión
expresion = (nota1 + nota2) * (nota3 + nota4)/2;
console.log("El resultado de la expresión, ((nota1 + nota2) * (nota3 + nota4)/2), es:", expresion);

// Fin